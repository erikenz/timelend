/** @type {import('next').NextConfig} */
const nextConfig = {
  allowedDevOrigins: [
    "http://127.0.0.1:3001",
    "http://localhost:3000",
    "http://localhost:3001",
  ],
};

export default nextConfig;
